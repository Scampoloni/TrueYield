# Disable JWT validation in tests — SecurityConfig permits all when this is empty
spring.security.oauth2.resourceserver.jwt.issuer-uri=

# Static URI for tests — no real connection needed when services are mocked
spring.mongodb.uri=mongodb://localhost:27017/trueyield-test
