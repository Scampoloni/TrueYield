package ch.zhaw.trueyield.model.enums;

/** A descriptive, non-regulatory signal calculated from collected evidence only. */
public enum EsgEvidenceSignal {
    FAVOURABLE,
    MIXED,
    ADVERSE,
    INSUFFICIENT_EVIDENCE
}
